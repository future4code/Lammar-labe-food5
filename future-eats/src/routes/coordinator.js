export const goToLoginPage = (navigator) => {
    navigator('/login');
}

export const goToSignupPage = (navigator) => {
    navigator('/signup');
}

export const goToFeedPage = (navigator) => {
    navigator(`/feed/${id}`);
}

export const goToPlacesPage = (navigator) => {
    navigator(`/places/${id}`);
}

export const goToProfilePage = (navigator) => {
    navigator(`/profile/${id}`);
}

export const goToCartPage = (navigator) => {
    navigator('/cart');
}